{
  "name": "turbo-cats",
  "version": "1.0.0",
  "description": "Turbo Cats - 16-bit side scrolling runner",
  "main": "index.js",
  "scripts": {
    "build:android": "npx cap sync android && npx cap build android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
